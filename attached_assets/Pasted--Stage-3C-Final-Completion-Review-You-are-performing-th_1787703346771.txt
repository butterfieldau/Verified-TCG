# Stage 3C — Final Completion Review

You are performing the **final release-readiness review of Stage 3C**.

Do not redesign the catalogue architecture.

Do not begin Stage 3D.

Do not merge to `main` unless this review explicitly concludes there are no remaining Stage 3C release blockers.

---

# TARGET REVISION

Repository:

`butterfieldau/Verified-TCG`

Branch:

`stage-3c/internal-catalogue-read-cutover`

Expected final candidate commit:

`bbab38e`

First:

```bash
git fetch origin
git switch stage-3c/internal-catalogue-read-cutover
git reset --hard origin/stage-3c/internal-catalogue-read-cutover
git rev-parse HEAD
git status --short
```

Confirm that HEAD is the full SHA corresponding to:

`bbab38e`

and the working tree is clean.

Do not continue against an older revision.

---

# CONTEXT

Stage 3C introduces a feature-flagged canonical catalogue read layer.

Architecture:

```text
existing app request
      ↓
CatalogueReadService
      ↓
canonical internal catalogue
      ↓
usable result?
 YES → canonical public DTO
 NO  → existing JustTCG fallback
```

Important:

* JustTCG remains active.
* Collection IDs remain unchanged.
* Wishlist IDs remain unchanged.
* Scanner recognition remains unchanged.
* PriceCharting remains unchanged.
* Production feature flag remains disabled.
* No production deployment has occurred.

---

# PREVIOUS ACCEPTANCE STATUS

Previous runtime acceptance reached:

* Full API suite: **422 tests**
* Failures: **0**
* Recursive repository tests: passed
* Workspace typecheck: passed
* DB build: passed
* API production build: passed
* Isolated disposable PostgreSQL acceptance: passed
* Feature flag OFF behaviour: passed
* Feature flag ON canonical-first behaviour: passed
* Fallback behaviour: passed
* Unsupported-card fallback: passed
* Search/alias/collector/set-code behaviour: passed
* DTO shadow comparison: passed with explicit mismatch reporting

Several real blockers were discovered during review and subsequently fixed:

1. Public canonical/variant UUID leakage
2. Unstable merged search pagination
3. Incomplete shadow/metric semantics
4. Empty provider page erasing canonical results
5. Canonical-only search matches lost during partial provider overlap
6. Unbounded in-process read metrics

The candidate `bbab38e` is expected to contain all of those fixes.

---

# PRIMARY GOAL

Determine one thing:

**Is Stage 3C safe to merge into `main` as a disabled-by-default, canonical-first catalogue read capability with JustTCG fallback?**

This is not a new implementation stage.

Only fix genuine Stage 3C release blockers discovered during this final review.

---

# 1. REVIEW THE EXACT DIFF

Compare:

`main`

against:

`stage-3c/internal-catalogue-read-cutover`

Inspect every Stage 3C change.

Confirm there is no unrelated feature work.

Pay particular attention to:

* canonical read service
* public DTO mapper
* search result merge logic
* pagination
* fallback logic
* feature flag
* telemetry/metrics
* card detail
* images
* variants
* aliases
* scanner-facing card resolution
* collection-facing resolution
* route changes
* tests

---

# 2. PUBLIC IDENTITY LEAK CHECK

Confirm **no internal canonical UUID is exposed as a replacement public ID**.

Inspect:

* root card DTO
* set DTO
* variants
* images
* nested objects
* search results
* card detail results

Existing public/provider-style card identity must remain backwards-compatible.

Internal UUIDs may remain server-side.

If any internal ID is still exposed unexpectedly, treat it as a release blocker and fix it.

---

# 3. FEATURE FLAG

Verify:

`CANONICAL_CATALOGUE_READS_ENABLED`

is:

* server-side only
* default OFF
* not exposed through Expo/public env
* capable of disabling canonical reads without a deployment

### Flag OFF

Must behave as the pre-Stage-3C JustTCG flow.

### Flag ON

Must use:

canonical-first → JustTCG fallback.

---

# 4. CARD LOOKUP

Verify mapped cards:

* resolve canonically
* preserve existing public card ID
* retain correct name
* collector number
* set
* game
* rarity
* image

Verify fallback when:

* no canonical mapping
* canonical row incomplete
* image requirements make canonical result unusable
* unsupported game/source record
* canonical DB read errors

Canonical problems must not unnecessarily break card browsing.

---

# 5. UNSUPPORTED RECORDS

Explicitly test known unsupported/provider-variable records, especially Japanese Pokémon records from Stage 3B.

These must:

* continue through JustTCG fallback
* not disappear
* not be assigned a fabricated game
* not produce fake canonical records

---

# 6. SEARCH

Test:

* exact name
* partial name
* normalised name
* alias
* collector number
* set name
* set code

Verify canonical and provider results merge safely.

Critical requirements:

* canonical-only valid results must not disappear
* partial provider overlap must not erase canonical results
* duplicate canonical/provider records must be deduplicated
* provider-established pagination/order should remain stable where applicable
* empty provider page must not wipe valid canonical results

Test multiple pages if the route supports pagination.

---

# 7. DTO SHADOW CHECK

Using isolated non-production data, compare canonical public DTOs with the equivalent JustTCG records.

Compare:

* public ID
* name
* game
* set name
* set code
* collector number
* rarity
* primary image

Report differences explicitly.

Do not silently normalise away material mismatches.

---

# 8. VARIANTS

Verify canonical variants do not leak internal UUIDs.

Check:

* variant DTO fields
* finish
* edition
* stamp
* language
* region

Only public-safe fields should leave the API.

---

# 9. IMAGES

Verify:

* canonical image used when valid
* provider fallback used when canonical image missing/incomplete
* malformed/credential-bearing URLs rejected
* no internal DB IDs exposed

---

# 10. SCANNER

Do not modify recognition.

Verify scanner:

* quota unchanged
* recognition unchanged
* recognised existing card ID resolves through catalogue read layer safely
* supported card can display canonical data
* unsupported card still displays using fallback

---

# 11. COLLECTION

Verify no collection identity migration occurred.

Existing collection records must retain:

* existing `cardId`
* grading information
* condition
* quantity
* acquisition price
* acquisition currency
* acquisition date
* For Sale
* For Trade
* notes

Only display/catalogue resolution may use canonical reads.

---

# 12. WISHLIST

Confirm existing wishlist identity remains unchanged.

No canonical UUID migration should occur.

---

# 13. PRICECHARTING

Confirm no regression to:

* provider mappings
* quote resolution
* history
* portfolio valuation
* grade mapping
* pricing routes

PriceCharting must not become dependent on canonical UUIDs during Stage 3C.

---

# 14. TRENDING / GAINERS / LOSERS

Verify current truthful market behaviour remains intact.

Do not allow Stage 3C to fabricate movement from incomplete canonical data.

Canonical metadata enrichment is acceptable.

Provider/market logic should remain unchanged unless explicitly tested.

---

# 15. TELEMETRY / METRICS

Verify Stage 3C metrics are:

* bounded
* non-PII
* safe over long-running process lifetime

At minimum track:

* canonical hits
* JustTCG fallbacks
* canonical read errors
* incomplete-canonical fallback
* unsupported fallback

Verify percentages cannot exceed 100% or become misleading due to double counting.

If process-local counters exist, ensure they have bounded memory behaviour.

---

# 16. CANONICAL DATABASE FAILURE

Simulate canonical DB read failure.

The app should:

* use JustTCG fallback where possible
* not expose SQL/database details
* record sanitised telemetry
* remain usable

---

# 17. PERFORMANCE

Using isolated acceptance data, record representative:

* mapped card lookup latency
* fallback lookup latency
* canonical search latency
* fallback/merged search latency

We do not need benchmark perfection.

Look for obvious N+1 behaviour or pathological query patterns.

---

# 18. FRESH DATABASE BOOTSTRAP

Run the approved Stage 3A.1 fresh-database bootstrap on a disposable DB.

Confirm Stage 3C changes did not break fresh installs.

Do not use production.

---

# 19. FULL REGRESSION MATRIX

Run:

* Stage 3A focused tests
* Stage 3B focused tests
* Stage 3C focused tests
* JustTCG/catalogue provider tests
* scanner
* collection
* pricing
* pricing routes
* eBay
* telemetry
* auth
* subscriptions
* admin/governance
* events
* trust
* complete API suite
* recursive repository suite

Current expected minimum:

**422 tests / 0 failures**

If new regression tests are added during this final review, final total should be greater than 422.

---

# 20. BUILD VERIFICATION

Run:

* DB build
* workspace TypeScript
* API production build
* web build where supported

The unrelated Expo/mockup non-interactive port collision previously observed should be documented separately if it still exists.

Do not classify that as a Stage 3C blocker unless evidence shows Stage 3C caused it or it prevents the actual release build path.

---

# 21. SECURITY

Scan for:

* DB URLs in clients
* JustTCG keys in clients
* PriceCharting tokens in clients
* SQL errors exposed publicly
* internal UUID leakage
* PII in read telemetry
* credential-bearing image URLs

No secrets may be introduced by Stage 3C.

---

# 22. PRODUCTION SAFETY

Confirm:

* production DB was not used for destructive acceptance
* production user data was not modified
* production feature flag was not enabled
* no production catalogue cutover occurred
* JustTCG remains available

---

# 23. FIX POLICY

If this review finds a genuine **high or medium Stage 3C release blocker**:

* fix it on this branch
* add regression coverage
* rerun the relevant acceptance checks
* rerun the complete API suite
* commit the fix

Do not expand into unrelated refactors.

Do not begin Stage 3D.

---

# 24. COMPLETION REVIEW

After all checks, perform one final independent review of the resulting Stage 3C diff.

The review must answer:

* any remaining high severity issues?
* any remaining medium severity issues?
* any backwards compatibility problems?
* any data-loss risk?
* any availability/fallback risk?
* any public internal-ID leakage?
* any unbounded-memory issue?
* any search-result loss/pagination issue?

If any remain, do not approve the merge.

---

# REQUIRED FINAL REPORT

Return:

1. Branch
2. Starting `main` SHA
3. Final Stage 3C SHA
4. Working-tree status
5. Files changed
6. Commits included
7. Feature flag result
8. Flag OFF behaviour
9. Flag ON behaviour
10. Public ID compatibility
11. Internal UUID leakage result
12. Variant UUID leakage result
13. Canonical card lookup
14. Card fallback
15. Unsupported-card fallback
16. Canonical DB failure fallback
17. Name search
18. Alias search
19. Collector-number search
20. Set search
21. Set-code search
22. Partial-overlap search behaviour
23. Canonical-only search preservation
24. Empty-provider-page behaviour
25. Pagination stability
26. Search deduplication
27. Image behaviour
28. DTO shadow result
29. DTO mismatches
30. Scanner regression
31. Collection regression
32. Wishlist regression
33. PriceCharting regression
34. Pricing-route regression
35. Trending/gainers/losers result
36. Telemetry result
37. Metrics bounding result
38. Canonical hits
39. JustTCG fallbacks
40. Fallback percentage
41. Canonical errors
42. Unsupported fallbacks
43. Lookup latency
44. Search latency
45. Stage 3A test result
46. Stage 3B test result
47. Stage 3C test result
48. Full API test total
49. Full API failures
50. Recursive repository result
51. Workspace TypeScript
52. DB build
53. API production build
54. Fresh bootstrap
55. Security result
56. Production DB touched
57. Production user data modified
58. Production flag enabled
59. Production cutover performed
60. JustTCG still active
61. Remaining high issues
62. Remaining medium issues
63. Remaining low issues
64. Final independent review verdict
65. Safe/not safe to merge Stage 3C
66. Safe/not safe to enable canonical reads later through a controlled rollout
67. Next release-critical action

---

# MERGE RULE

If and only if:

* final full test suite passes
* builds pass
* fresh bootstrap passes
* no remaining high/medium Stage 3C issue exists
* backwards compatibility is preserved
* JustTCG fallback works
* final independent review approves

then report:

**STAGE 3C APPROVED FOR MERGE**

Do NOT merge automatically unless explicitly instructed after the report.

Do not begin another catalogue architecture stage.

After Stage 3C approval, the next work should move to the **Verified TCG release-readiness audit and launch blockers**, not another speculative migration stage.
