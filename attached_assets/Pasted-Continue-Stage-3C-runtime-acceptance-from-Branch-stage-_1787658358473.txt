Continue Stage 3C runtime acceptance from:
Branch: stage-3c/internal-catalogue-read-cutover
Expected HEAD:
2d20d9075a5240c38a94561df114d220b330b672
Fetch and check out that exact revision. Do not merge to main.
Use isolated non-production PostgreSQL infrastructure. Recreate or reuse a disposable canonical catalogue database populated through the approved Stage 3B cache-backed import. Do not run destructive tests against HeliumDB or production.
Complete Stage 3C acceptance end-to-end.
First test with CANONICAL_CATALOGUE_READS_ENABLED=false and prove current JustTCG behaviour is unchanged.
Then enable CANONICAL_CATALOGUE_READS_ENABLED=true only in the disposable acceptance environment and verify:
mapped card lookup is served from the canonical catalogue
public JustTCG-style card ID remains unchanged
canonical UUID stays internal/backwards-compatible
canonical search works by name
alias search works
collector-number search works
set name/code search works
primary canonical images resolve correctly
incomplete canonical records fall back safely
unmapped records fall back to JustTCG
unsupported Japanese Pokémon records still work through fallback
canonical DB errors fall back rather than breaking catalogue browsing
duplicate canonical/JustTCG results are deduplicated
Perform DTO shadow comparisons against real mapped source records for name, external ID, set, collector number, rarity and image. Report exact mismatches rather than hiding them.
Measure actual runtime:
canonical hits
JustTCG fallbacks
canonical errors
unsupported-record fallbacks
canonical hit percentage
fallback percentage
representative lookup latency
representative search latency
Verify scanner behaviour remains unchanged. Scanner recognition itself must not be rewritten; only the resulting catalogue display may resolve canonical-first.
Verify collection records remain unchanged, including card IDs, grading data, quantity, acquisition price/currency, For Sale and For Trade.
Verify wishlist IDs remain unchanged.
Verify PriceCharting mappings and pricing behaviour remain unchanged.
Verify trending/gainers/losers remain truthful and retain the existing provider behaviour where canonical data is insufficient.
Run the complete validation matrix:
Stage 3A tests
Stage 3B tests
Stage 3C tests
JustTCG/catalogue provider tests
scanner
collection
pricing
pricing routes
eBay
telemetry
auth
subscription
admin/governance
events
trust
full API suite
recursive repository suite
workspace typecheck
DB build
API production build
fresh database bootstrap
Stage 3B baseline is 411 tests / 90 suites / 0 failures. Stage 3C must finish with more tests than that and 0 failures.
Fix genuine Stage 3C defects discovered during runtime acceptance on this branch and rerun until green.
Do not deploy the feature flag to production.
Do not merge to main.
Do not remove JustTCG.
Do not migrate collection or wishlist IDs.
Do not begin Stage 3D.
Return the final Stage 3C runtime acceptance report with the final commit SHA and recommendation on whether it is safe to merge and enable canonical reads for a controlled release.